info:
  name: get destination by id
  type: http
  seq: 1

http:
  method: GET
  url: "{{local-laravel}}/api/destinations/1"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
