info:
  name: search destination
  type: http
  seq: 4

http:
  method: GET
  url: "{{local-laravel}}/api/destinations?category=alam"
  params:
    - name: category
      value: alam
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: di endpoint ini harus menjalankan seeder terlebih dahulu baru bisa menggunakan fitur search
