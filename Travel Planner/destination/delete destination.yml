info:
  name: delete destination
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{local-laravel}}/api/destinations/100"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
