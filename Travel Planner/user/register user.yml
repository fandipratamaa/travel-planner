info:
  name: register user
  type: http
  seq: 1

http:
  method: POST
  url: "{{local-laravel}}/api/users"
  body:
    type: json
    data: |-
      {
        "name": "Test",
        "email": "test@example.com",
        "password": "test123",
        "password-confirmation": "test123" 
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
